<li id="comment-<%= comment._id %>" >
    <p>
        <%= comment.content %>
            <br>
            
            <small>
                <%= comment.user.accountHolder %>
            </small>
            <!-- <small>
                <% if (locals.user) { %>
            <a href="/likes/togglelike/?id<%= comment._id %>&type=Post" data-like="<%= comment.likes.length %>" class="toggle-like-btn">
                <i class="fa-regular fa-thumbs-up likes-symbol" ></i> <%= comment.likes.length %>
            </a>
            <% } else { %>
               Likes  <%= comment.likes.length %>
            <% } %>
            <br>
        </small> -->
        <small>
            <% if (locals.user){ %>
                <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
                        <%= comment.likes.length %> Likes
                </a>
            <% }else{ %>
                <%= comment.likes.length %> Likes
            <% } %>
        </small>
            <% if (locals.user && locals.user.id==comment.user.id){ %>
                <small>
                    <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>" )">X Delete comment</a>
                </small>
                <% } %>
    </p>
</li>